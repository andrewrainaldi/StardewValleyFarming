package src;

public class Main {
    public static void main(String[] args) {
        // Crea un'istanza della classe GameApp
        GameApp game = new GameApp();
        
        // Se GameApp ha un metodo pubblico per avviare il gioco (es. start()), chiamalo qui
        // game.start(); 
        
        System.out.println("GameApp è stata istanziata correttamente.");
    }
}